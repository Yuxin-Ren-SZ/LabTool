{
  "name": "labtools",
  "version": "0.0.0",
  "private": true,
  "description": "Zero-dependency static HTML tools for cell-biology bench work. Runtime tools ship with NO dependencies; only the dev/test tooling below uses npm.",
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test:e2e": "node --test --test-concurrency=1 tests/e2e/*.test.mjs",
    "test:e2e:update": "UPDATE_SNAPSHOTS=1 node --test --test-concurrency=1 tests/e2e/*.test.mjs",
    "fixtures:check": "node scripts/gen-fixtures.mjs --check",
    "fixtures:write": "node scripts/gen-fixtures.mjs"
  },
  "devDependencies": {
    "puppeteer": "^24.15.0"
  }
}
