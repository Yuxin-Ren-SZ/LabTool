{
  "name": "labtools-desktop",
  "productName": "LabTools",
  "version": "0.1.0",
  "private": true,
  "description": "Electron desktop shell for LabTools (Windows/macOS/Linux). Serves the zero-dependency web assets over a privileged app:// scheme; the web tree remains the single source of truth.",
  "type": "module",
  "main": "src/main.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "electron": "43.4.0",
    "playwright": "1.62.1"
  }
}
